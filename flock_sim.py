#!/usr/bin/env python

class Flock(object):
   """Takes in an array of birds"""
   def __init__(self, birds):
      self.birds=birds

   def get_com(self):
      self.pos
      return com

   def update(self):
      for bird in self.birds:
         bird.update(target)
      

class Bird(object):
   def __init__(self, pos=np.array([0,0,0], dtype=float32), vel = np.array([0,0,0], dtype=float32) ):
      self.pos = pos
      self.vel = vel

   def get_pos(self):
      return self.pos

   def update(self, target):
#feed target update velcoty in direction of target


#agent based modling 
